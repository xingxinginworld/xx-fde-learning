# ── 密钥与真实配置（绝不入库）────────────────────────────
.env
.env.*
!.env.template
*.key
secrets.json

# ── Python ───────────────────────────────────────────
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# ── 运行时生成物（索引含真实分块，不公开）──────────────
rag_demo/index/
rag_demo/data/
!rag_demo/data/.gitkeep

# ── 证书注册表（含真实姓名，由群主本地维护，不公开）────
certs/registry.json
# 导出的单张证书 HTML（按需本地生成，不公开）
certs/issues/

# ── Capstone 评分台账与学员提交（含真实姓名/作品，不公开）──
capstone/results.json
capstone/submissions/

# ── 系统/编辑器 ───────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/

# ── 构建产物 ──────────────────────────────────────────
*.log
dist/
build/
